export enum TecnicoEstado {
  disponible = 'disponible',
  ocupado = 'ocupado',
  ausente = 'ausente',
}

export enum AsignacionEstado {
  reservado = 'reservado',
  confirmado = 'confirmado',
  liberado = 'liberado',
}
